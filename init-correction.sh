#!/bin/bash

# Do a find command to note all *.o *.hex etc files

# Maybe checout the last commit made before the due date

# Create a branch,

# Drop a correction file in the right place (use my gen-correction-file script
# but modify it so that it takes only parameters and has the file content as a
# string inside the gen-correction-script file itself.


